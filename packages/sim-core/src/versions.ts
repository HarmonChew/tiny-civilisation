/** Changes only when deterministic simulation outcomes intentionally change. */
export const SIMULATION_BEHAVIOR_VERSION = 1 as const;

/** Version of the authoritative state stored inside SimulationState. */
export const SIMULATION_STATE_VERSION = 1 as const;

/** Public transport-contract versions. */
export const COMMAND_SCHEMA_VERSION = 1 as const;
export const SNAPSHOT_SCHEMA_VERSION = 1 as const;
export const REPLAY_SCHEMA_VERSION = 1 as const;
export const SAVE_SCHEMA_VERSION = 1 as const;
